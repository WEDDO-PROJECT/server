const router = require('express').Router();
const adminController = require("../controllers/checklist.js");

router.post("/addinChecklist", checklist.selectAlltasks)
router.get("/allTasks", checklist.)